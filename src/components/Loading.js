import React from "react";
//styles
import "../css/Loading.css";

const Loading = () => (
  <div className="loadingContainer">
    <div className="loading">
      <p className="loading__message">Cargando...</p>
    </div>
  </div>
);

export default Loading;
